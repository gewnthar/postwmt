# Flask Application Configuration
SECRET_KEY=your_very_secret_flask_key_here
# Example for SQLAlchemy Database URL (MariaDB/MySQL)
# Replace with your actual database connection string
# For local development with pymysql:
# DATABASE_URL="mysql+pymysql://user:password@host/database_name"
# For production on AlmaLinux with MariaDB (socket-based perhaps):
# DATABASE_URL="mysql+pymysql://your_db_user:your_db_password@localhost/your_db_name"
DATABASE_URL="mysql+pymysql://postwmt_user:your_password@localhost/postwmt_db"

# Google OAuth Credentials
# Get these from Google Cloud Console (APIs & Services > Credentials)
GOOGLE_CLIENT_ID="your_google_client_id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your_google_client_secret"

# Application Base URL (IMPORTANT for OAuth redirects)
# This should be the URL where your application is publicly accessible.
# For local development:
# APP_BASE_URL="http://localhost:5000"
# For production (replace with your actual domain and include https if used):
# APP_BASE_URL="https://yourdomain.com"
APP_BASE_URL="http://localhost:5000"

# Flask Environment (optional, 'development' enables debug mode)
# FLASK_APP=postwmt_app.py
# FLASK_ENV=development
# FLASK_DEBUG=1 # Alternative to FLASK_ENV=development for debug mode

# Logging configuration (optional examples)
# LOG_LEVEL=INFO # or DEBUG, WARNING, ERROR
# LOG_FILE_PATH=/var/log/postwmt/app.log # Ensure directory exists and has write permissions
# GUNICORN_LOG_LEVEL=info
# GUNICORN_ACCESS_LOG_FILE=/var/log/postwmt/gunicorn_access.log
# GUNICORN_ERROR_LOG_FILE=/var/log/postwmt/gunicorn_error.log
